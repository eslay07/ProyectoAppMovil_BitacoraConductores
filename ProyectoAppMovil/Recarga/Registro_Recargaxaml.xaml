<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoAppMovil.Registro_Recargaxaml">
    <ContentPage.Content>
        <StackLayout BackgroundColor="LightBlue"
                     Margin="5">
            <Label Text="Lista de Recargas"
                   HorizontalOptions="Center"
                   BackgroundColor="WhiteSmoke"/>
            <ListView x:Name="ListaRecargas">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Fecha_Recarga}" Detail="{Binding galones_Recarga} " ></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>